---
interface Props {
  name: string;
  description: string;
}

const props = Astro.props;
---

<div>
  <h3 class="line-clamp-1 text-sm">- {props.name}</h3>
  <p class="text-muted-foreground line-clamp-2 text-xs">{props.description}</p>
</div>
